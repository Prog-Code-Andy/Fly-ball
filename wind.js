/* function StyleBall(){

    this.printHtmlBall = function(x, y, w, h, color){
        let field = document.querySelector('body');
        let tempBall = document.createElement('div');
        tempBall.className = ball;
        tempBall.style.backgroundColor = color;
        tempBall.style.left = x;
        tempBall.style.top = y;
        tempBall.style.width = w;
        tempBall.style.height = h;
        field.append(tempBall);
    }
    
}

 */